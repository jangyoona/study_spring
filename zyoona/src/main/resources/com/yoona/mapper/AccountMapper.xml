<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yoona.mapper.AccountMapper">
	
	<select id="selectLoginMember" parameterType="MemberDto" resultType="MemberDto">
		SELECT memberId, email, userType, regDate, active
		FROM member
		WHERE memberId = #{ memberId } AND passwd = #{ passwd }
	</select>
	
	<insert id="insertMember" parameterType="MemberDto">
		INSERT INTO member(memberId, passwd, email) VALUES(#{ memberId }, #{ passwd }, #{ email })
	</insert>
	
	<select id="selectRegisterMemberId" parameterType="string" resultType="int">
		SELECT COUNT(memberId) FROM member WHERE memberId = #{ memberId }
	</select>
	

</mapper>